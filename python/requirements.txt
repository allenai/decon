# Python dependencies for decon orchestration
boto3>=1.26.0
pyyaml>=6.0
requests>=2.28.0
smart-open>=6.0.0

# Optional dependencies for reference scripts
ray>=2.0.0  # Only needed for reference orchestration scripts
click>=8.0.0  # Only needed for reference CLI scripts